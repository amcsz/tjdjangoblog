# django blog

## Instructions to run
Run the following commands to setup the environment:
```bash
chmod +x ./setup.sh
./setup.sh
```
To run the app, run:
```bash
python djangoblog/manage.py runserver
```